namespace WebView2.Markup
{
	/// <summary>
	/// The hr element represents a paragraph-level thematic break, e.g. a scene change in a story, or a transition to another topic
	/// within a section of a reference book.
	/// </summary>
	public sealed class hr : HTMLElement, FlowContent, addressContent, header_footer_Content, dtContent { }
}
